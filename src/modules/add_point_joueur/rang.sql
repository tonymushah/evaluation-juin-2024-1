SELECT
	RANK() OVER (
		ORDER BY
			TEMPS ASC
	) RANG,
	ETAPE,
	COUREUR,
	EQUIPE,
	EQUIPE_COUREUR,
	POINTS
FROM
	V_TEMPS_COUREUR_ETAPE_EQUIPE_COUREUR
WHERE
	ETAPE = {}
	AND TEMPS IS NOT NULL
GROUP BY
	ETAPE,
	TEMPS,
	COUREUR,
	EQUIPE,
	EQUIPE_COUREUR,
	POINTS